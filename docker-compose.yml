services:
  dubbing-backend:
    build: .
    container_name: ai-video-dubbing-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
